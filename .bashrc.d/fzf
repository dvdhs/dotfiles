# try to source files files that are possibly provided by the
# packages. Mostly for fzf.
for base in ~/.nix-profile ~/.local; do
  KEYBIND_FILE="$base/share/fzf/key-bindings.bash"
  [ -f "$KEYBIND_FILE" ] && source "$KEYBIND_FILE"
done
